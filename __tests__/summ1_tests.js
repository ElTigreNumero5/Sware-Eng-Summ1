// import my functions
import { getAttendData, buildChart} from '../functions.js';


// smoke test
test('when pressed to attest is there Jest test quest success?', () => {
    expect(2-1).toBe(1);
})




describe('tests all functions', () => {
    test('that the function outputs three arrays', () => {
        expect(getAttendData())

    })
})